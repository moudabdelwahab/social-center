'use strict';
/* إعداد الاتصال بمشروع Supabase — المفتاح العام (anon) فقط، ولا يُوضع أي Service Key هنا أبدًا.
   الحماية الحقيقية تتم عبر RLS في قاعدة البيانات وليس عبر إخفاء المفتاح. */
const SCC_SUPABASE_URL = 'https://iacaestpsarreiijkxag.supabase.co';
const SCC_SUPABASE_ANON_KEY = 'PASTE_YOUR_ANON_KEY_HERE'; // Settings → API → anon public key
